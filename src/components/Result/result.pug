div(dir="ltr").result-container.d-flex.flex-column.align-items-center.text-center
    .status-icon-wrapper
        i(v-if="result === true").fas.fa-check-circle.icon-accept.fa-10x
        i(v-if="result === false").fas.fa-times-circle.icon-reject.fa-10x
    .status-text-container.mt-4
        p(v-if="result === true").status-text.color-success !رشته پذیرش شد
        p(v-if="result === false").status-text.color-reject !رشته پذیرش نشد

.navigation-buttons-container.d-flex.justify-content-around
    .back-button-container.mx-2
        button(@click="goBack").button.back-button.d-flex.align-items-center.cursor-pointer
            i.fas.fa-chevron-right.ml-2
            span.button-title.mt-1 صفحه قبل

    .home-button-container.mx-2
        button(@click="goHome").button.home-button.d-flex.align-items-center.cursor-pointer
            i.fas.fa-home.ml-2
            span.button-title.mt-1 صفحه اول
